package tech.chris.leetcode;

/**
 * You have n dice and each die has k faces numbered from 1 to k.
 * <p>
 * Given three integers n, k, and target, return the number of possible ways (out of the k^n total ways) to roll the
 * dice so the sum of the face-up numbers equals target. Since the answer may be too large, return it modulo 10^9 + 7.
 * <p>
 * 1 <= n, k <= 30
 * 1 <= target <= 1000
 */
public class NumberOfDiceRollsWithTargetSum {

    private static final int MODULO = 1_000_000_007;

    public int numRollsToTarget (int n, int k, int target) {
        // TODO:
        throw new UnsupportedOperationException();
    }
}
